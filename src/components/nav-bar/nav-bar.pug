include ../nav-bar-item/nav-bar-item

mixin navBar(params = {})
  -
    const { itemList, display = 'vertical' } = params;
    let classList = 'nav-bar';
    if (display === 'vertical') {
      classList += ' nav-bar_vertical';
    }

  ul(class=classList)
    each item in itemList
      +navBarItem(item)
