mixin toggleButton(params)
  -
    var {label, checked= false, name="label"} = params

  label.toggle_button.toggle_button_label=label
    input.toggle_button__input(type="checkbox" name=name checked=checked)