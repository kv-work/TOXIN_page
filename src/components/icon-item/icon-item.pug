mixin iconItem(params = {})
  -
    const { icon, label, description } = params;

  .icon-item
    .icon-item__icon
      i.material-icons= icon
    .icon-item__text
      p.icon-item__label= label
      p.icon-item__description= description
