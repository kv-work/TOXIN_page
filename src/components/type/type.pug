include ./type_example/type_example

.type
  +typeExample("H1", "This one is the sub-section or widget title")
  +typeExample("H2", "Next one is the item title inside widgets")
  +typeExample("H3", "This is a label or CTA text")
  +typeExample("Body", "This is the body text which is used for most of the design, like paragraphs, lists, etc.")